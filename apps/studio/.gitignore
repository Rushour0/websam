node_modules/
dist/
.DS_Store
*.tsbuildinfo
coverage/
playwright-report/
test-results/

# Model weights are staged locally via `pnpm setup-weights`; never commit
# binaries into the app bundle source tree.
public/models/
